setup.txt
outputs/
teleop/mirror/mirror_calibration.json
# Isolated placo IK venv (see teleop/robot/kinematics.py). Recreate with:
#   uv venv --python 3.12 .venv-ik && uv pip install --python .venv-ik/bin/python --link-mode=copy placo
.venv-ik/
.claude/
__pycache__/
*.safetensors

# RL training checkpoints (SAC models + replay buffers) — large binaries.
teleop/rl_reach/checkpoints/

# MediaPipe Tasks model bundles: 22MB of binaries, reproducible via
# teleop/scripts/download_models.sh. Fetch them, don't commit them.
teleop/models/*.task
