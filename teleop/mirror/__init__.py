"""Live mirror: links the vision pose estimator to the robot safety core.

A third, standalone entry point (`python -m teleop.mirror.server`) that imports
teleop.vision and teleop.robot.control without modifying either. See README.md.
"""
