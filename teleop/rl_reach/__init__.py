"""RL reaching task on the physical SO-101 (architecture shakedown for grasp RL).

Standalone from-scratch FK (`fk.py`) + a Gymnasium env over the existing
`teleop.robot.control.RobotController` safety layer. See README.md.
"""
